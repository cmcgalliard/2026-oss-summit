apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: default
  namespace: demo-app
spec:
  gatewayClassName: nginx
  listeners:
    - name: web
      protocol: HTTP
      port: 8000
