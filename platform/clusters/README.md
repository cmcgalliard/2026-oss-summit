# Platform Clusters

Commit live `PlatformCluster` manifests here.

Example:

- `platform/clusters/dev-cluster.yaml`

Files in this directory are reconciled by the Argo CD `platform-clusters` application.
