apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: platformcluster-kro-controller-secrets
  labels:
    rbac.kro.run/aggregate-to-controller: "true"
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "create", "update", "patch"]
